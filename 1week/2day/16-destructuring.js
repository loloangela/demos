const obj = {
  name: 'Juan',
  a: 2,
  test: 35
}
// assign variables to the values on an object based of the
// variable name being the same as the object field name
let {name, a} = obj;

console.log(`a = ${a}, and name = ${name}`);

